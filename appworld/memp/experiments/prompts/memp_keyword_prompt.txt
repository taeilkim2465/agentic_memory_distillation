You are a keyword extractor. Given a task description, extract the most important keywords that capture the core entities, actions, and domain concepts.

These keywords will be used to retrieve relevant past experience from memory, so focus on terms that distinguish this task from others and are likely to appear in similar tasks.

Task: {{task}}

Output ONLY a valid JSON object with a single "keywords" field containing a list of 5-10 keywords (strings). No markdown, no explanation.

Example output:
{"keywords": ["spotify", "playlist", "count", "music", "library"]}
